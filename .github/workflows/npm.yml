
name: unitTest

on: [push, pull_request]

jobs:     
  unit-test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x, 15.x]

    steps:
      - run: npm test:unit